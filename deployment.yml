apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app_nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app4
  template:
    metadata:
      labels:
        app: my-app_nginx
    spec:
      containers:
        - name: my-container
          image: nginx
          ports:
            - containerPort: 80
