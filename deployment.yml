apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app4
  template:
    metadata:
      labels:
        app: my-app4
    spec:
      containers:
        - name: my-container
          image: nginx
          ports:
            - containerPort: 80
